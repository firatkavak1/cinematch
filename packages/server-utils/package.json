{
  "name": "server-utils",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "shared": "workspace:*",
    "app-errors": "workspace:*",
    "fastify": "^4.28.0",
    "@fastify/cors": "^9.0.0",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
